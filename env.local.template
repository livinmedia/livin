# ==============================================================
# LIVIN + HOMES + LIVIN ECOSYSTEM — Environment Variables Vault
# P0-019 — Secure Credential Vault
# ==============================================================
# RULES — READ BEFORE EDITING:
#   1. NEVER commit this file to GitHub — it is in .gitignore
#   2. NEVER hardcode any value from this file in source code
#   3. NEVER share this file over email, Slack, or chat
#   4. All keys rotate through this file — update here first
#   5. Status legend: [ACTIVE] [PENDING] [NOT NEEDED]
# ==============================================================
# Last Updated: March 2026
# Owner: Anthony (Platform Lead)
# ==============================================================


# ── SUPABASE — DEV ────────────────────────────────────────────
# Project: livin-ecosystem-app | ID: bmemtekrchzoxpwtaufd
# [ACTIVE]
NEXT_PUBLIC_SUPABASE_URL=your-key-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-key-here
SUPABASE_SERVICE_ROLE_KEY=your-key-here
SUPABASE_AGENT_KEY=your-key-here


# ── SUPABASE — PRODUCTION ─────────────────────────────────────
# Project: livin-ecosystem-production | ID: ucqlfortlrlzrvmxchdy
# [PENDING — populate before production deploy]
NEXT_PUBLIC_SUPABASE_URL_PROD=your-key-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-key-here
SUPABASE_SERVICE_ROLE_KEY=your-key-here
SUPABASE_AGENT_KEY=your-key-here


# ── ANTHROPIC (CLAUDE AI) ─────────────────────────────────────
# console.anthropic.com | Used by all 16 AI agents via OpenClaw
# [ACTIVE]
ANTHROPIC_API_KEY=your-key-here


# ── NEXT.JS ───────────────────────────────────────────────────
# [ACTIVE]
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# On-demand revalidation webhook secret — P0-028
# [ACTIVE]
REVALIDATE_SECRET=livin-revalidate-2026


# ── GOOGLE PLACES API ─────────────────────────────────────────
# console.cloud.google.com | Used by City Content Builder Agent
# [PENDING — waiting on client]
GOOGLE_PLACES_API_KEY=



# ── ALLEVENTS.IN API ─────────────────────────────────────────
# allevents.in/api | Used by City Content Builder + Freshness Agent
# [PENDING — waiting on client]
ALLEVENTS_API_KEY=


# ── U.S. CENSUS BUREAU API ───────────────────────────────────
# api.census.gov | Free — used by City Content Builder Agent
# [PENDING — register at api.census.gov/data/key_signup.html]
CENSUS_API_KEY=


# ── UBERSUGGEST / NEIL PATEL API ─────────────────────────────
# Used by SEO Optimizer Agent
# [PENDING — waiting on client]
UBERSUGGEST_API_KEY=


# ── HOMESTACK ────────────────────────────────────────────────
# Used by H+L mobile app integration
# [PENDING — Anthony + HomeStack team coordination]
HOMESTACK_API_KEY=
HOMESTACK_APP_ID=
HOMESTACK_BASE_URL=


# ── OPENCLAW ─────────────────────────────────────────────────
# AI orchestration layer — connects all 16 agents
# [PENDING — OpenClaw framework setup in P0-039]
OPENCLAW_API_KEY=
OPENCLAW_WEBHOOK_SECRET=
OPENCLAW_BASE_URL=


# ── BRAND CONFIG ─────────────────────────────────────────────
# Used by middleware and brand-context utility
# [ACTIVE]
NEXT_PUBLIC_LIVIN_DOMAIN=livin.in
NEXT_PUBLIC_HL_DOMAIN=homesandlivin.in
NEXT_PUBLIC_APP_URL=http://localhost:3000


# ── INTERNAL SECRETS ─────────────────────────────────────────
# [ACTIVE]
# Used for API route authentication
INTERNAL_API_SECRET=your-internal-api-secret-here

# Cron job authentication (OpenClaw scheduled tasks)
CRON_SECRET=your-cron-secret-here
